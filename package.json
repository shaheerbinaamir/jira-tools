{
  "name": "mentions-only",
  "version": "1.0.0",
  "private": true,
  "description": "Chrome extension: a local inbox of Jira and Confluence comments that @-mention you.",
  "type": "module",
  "scripts": {
    "test": "node tests/mentions.test.mjs",
    "package": "rm -f mentions-only.zip && zip -r mentions-only.zip manifest.json src icons -x '*.DS_Store'"
  }
}
